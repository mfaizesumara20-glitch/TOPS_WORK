# Given the variable is_premium = 'True' (as a string), 
# write code to correctly convert it to a boolean value and print its type.
# The bool() function alone won’t work as expected. Think about string comparison.

is_premium = 'True'

hi = bool(is_premium)
print(hi)
print(type(hi))

